{
    "name": "react-electron-proj",
    "dependencies": {
        "body-parser": "^1.18.3",
        "express": "^4.16.3",
        "fs": "0.0.1-security",
        "mysql2": "^1.6.1",
        "path": "^0.12.7"
    },
    "devDependencies": {
        "babel-core": "^6.26.3",
        "babel-preset-es2015": "^6.24.1",
        "nodemon": "^1.18.3"
    },
    "scripts": {
        "build": "babel src -d build --presets=es2015",
        "start": "babel src -d build --presets=es2015&&node ./build/main.js",
        "development": "node ./scripts/development.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
